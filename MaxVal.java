package com.company;

/*public class MaxVal {
    public static int maxVal(int a, int b) {
        int x = Math.max(a,b);
        return x;
    }
    public static int maxVal(int a, int b, int c) {
        int x = Math.max(a,b);
        int y = Math.max(x,c);
        return y;
    }
}
*/

/*
public class MaxVal {
    public static double maxVal(int a, int b) {
        int x = Math.max(a,b);
        return x;
    }
    public static double maxVal(double a, int b) {
        double x = Math.max(a,b);
        double y = Math.max(x,c);
        return y;
    }

 */

public class MaxVal {
    public static double maxVal(int a, double b) {
        double x = Math.max(a,b);
        return x;
    }
//    public static double maxVal(double a, int b) {
//        double x = Math.max(a,b);
//       // double y = Math.max(x);
//        return y;
//    }

}