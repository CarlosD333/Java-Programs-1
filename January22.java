import java.util.Scanner;
public class January22
{
   public static void main( String[] args )
   {
      Scanner input = new Scanner ( System.in ); // SCanner to get input from the user
      
      int x;
      double y = 3.2;
      //x = y;
      x = (int)y;
      
      System.out.print( x );
      
      
      
      
      /*
      // Variable Declaration
      double x;
      double k = 17.0;
      int value = 3;
      
      System.out.println( "k is " + k );
      System.out.print( "Enter a decimal for x: " );
      x = input.nextDouble();
      
      System.out.println( "You entered " + x + " for x" );
      
      double sum = x + k;  
      System.out.print( "Adding " + x + " and " + k + ", is: " + sum + "." );
      System.out.print( " Program over." );
      System.out.println( " Have a good day!" );
      */
      
      }// Program execution ends
}